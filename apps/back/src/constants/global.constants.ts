/**
 * 响应成功错误码
 */
export const RESPONSE_SUCCESS_CODE = 200;

/**
 * 响应成功message
 */
export const RESPONSE_SUCCESS_MSG = 'success';

/**
 * 前端编辑操作时传递的 id，方便自定义验证器(UniqueConstraint)验证某个字段的唯一性
 *
 */
export const REQUEST_ENTITY_ID = '__id';

/**
 * 用作装饰器的键，用于在元数据中存储任务标记
 */
export const MISSION_DECORATOR_KEY = 'decorator:mission';
