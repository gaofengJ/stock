FROM node:18-alpine

# 设置工作目录
WORKDIR /usr/src/app

# 复制 package.json 文件到工作目录
COPY package.json ./

RUN npm install -g pnpm

# 安装项目依赖
RUN pnpm install

# 复制项目的所有文件到工作目录
COPY . .

# 暴露应用的端口
EXPOSE 3000

# 启动应用
CMD ["pnpm", "run", "start:prod"]
